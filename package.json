{
  "name": "test-workers",
  "version": "0.3.0",
  "private": false,
  "description": "A small Cloudflare Worker remote MCP server for LLM-triggered actions.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --keep-vars",
    "test": "npm run test:security && npm run test:oauth",
    "test:security": "node --test scripts/security.test.mjs",
    "test:oauth": "node --test scripts/oauth-integration.test.mjs",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types",
    "inspect": "npx @modelcontextprotocol/inspector@latest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "latest",
    "agents": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "latest",
    "wrangler": "latest"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "cloudflare-workers",
    "mcp",
    "model-context-protocol",
    "chatgpt",
    "automation",
    "llm-tools"
  ],
  "cloudflare": {
    "bindings": {
      "OAUTH_DB": {
        "description": "D1 database used by OneAIWorkers to store OAuth, connector registry, connector actions, and audit records. Cloudflare creates it automatically during deployment."
      },
      "MCP_SHARED_SECRET": {
        "description": "Required. A private password that protects OAuth connection and manual MCP access. Generate a long random value and keep it secret."
      }
    }
  }
}
